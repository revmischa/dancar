'use strict';

/**
 * @ngdoc function
 * @name Dancar.service:ExampleService
 * @description
 * # ExampleService
 */
angular.module('Dancar')
  // use factory for services
  .factory('ExampleService', function($rootScope, $location) {

    //$rootScope.changeView = function(view){
    //  $location.path(view); // path not hash
    //};

  });
